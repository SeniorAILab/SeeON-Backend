# Repository extraction provenance

This repository preserves the backend-owned history extracted from
`SeniorAILab/SeeON` main at
`f54f09864cee93fadb8744b34fc0531a33645ade`.

`seeon-commit-map.txt` is the authoritative `git filter-repo` old-to-new commit
map for the final comprehensive path and private-content rewrite. Source PRs
#675 through #678 are included; unmerged PR #679 is not included. Source release
tags were deliberately not transferred.

The source repository and its refs remain unchanged. Operator-personal content
was replaced only in the new rewritten history with typed neutral sentinels.
