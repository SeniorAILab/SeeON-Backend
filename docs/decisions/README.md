# Architecture Decision Records

An Architecture Decision Record (ADR) captures a significant technical choice made in this project — the context that forced the decision, the option selected, the alternatives weighed, and the trade-offs accepted. ADRs are the authoritative record of *why* the architecture looks the way it does, not just *what* it looks like.

## Lifecycle

```
Proposed  →  Accepted  →  Superseded
                       →  Deprecated
```

A decision moves from **Proposed** (drafted, under review) to **Accepted** (ratified and in effect) once the team agrees. If circumstances change and a new decision overrides an existing one, the old ADR is marked **Superseded** (with a reference to its successor) — it is never deleted. Removing an ADR would erase the reasoning trail; supersession preserves it. **Deprecated** is used when a decision is retired without a direct replacement (e.g., a component is removed).

> **Rule: never delete, always supersede.** Future readers need the historical chain to understand how the system evolved and why dead-end paths were rejected.

---

## Decision Index

| # | Title | Status | Date | Summary |
|---|-------|--------|------|---------|
| [ADR-001](./ADR-001-polyglot-monorepo.md) | Polyglot monorepo with per-ecosystem dependency management | Accepted | 2026-06-07 | pnpm workspace (front + backend) and uv project (ml) maintain separate lock files; the root `package.json` is an orchestration-only shell with no application dependencies. |
| [ADR-002](./ADR-002-postgres-everywhere.md) | PostgreSQL everywhere (dev/prod parity via DATABASE_URL) | Accepted | 2026-06-07 | Prisma's provider is baked into generated migrations, making SQLite-to-Postgres swaps unsafe at runtime; Docker Compose provides a local Postgres instance so dev and prod share a single provider and only differ by `DATABASE_URL`. |
| [ADR-003](./ADR-003-ml-serving-training-split.md) | ML serving/training lifecycle split + version-addressed artifacts | Accepted | 2026-06-07 | FastAPI serving and model training are treated as separate lifecycle concerns; artifacts are addressed as `ml/artifacts/<model-name>/<version>/` (Triton-inspired layout) so serving code can load a pinned version without touching training infrastructure. |
| [ADR-004](./ADR-004-relocate-video-data-to-ml-data.md) | Relocate video data from assets/ to ml/data/ | Accepted (partially superseded by ADR-012) | 2026-06-07 | Introducing `ml/` as the Python uv project that owns data processing makes `assets/` the wrong home for training footage; `ml/data/{raw,processed}` collocates data with the code that consumes it and keeps the `fall-video-crop-rename` skill paths coherent. |
| [ADR-005](./ADR-005-yolo26-pose-and-module-seam.md) | YOLO26-pose stack + two-seam module architecture | Accepted | 2026-06-08 | MediaPipe→Ultralytics YOLO26-pose (domain-fit partially verified); a `FrameSource` stream-seam unifies file + live stream and a `ModelModule.predict(frame)→DetectionResult` model-seam makes models pluggable. Complements ADR-003. |
| [ADR-006](./ADR-006-frame-source-intake-in-ml-util.md) | Frame-source intake in `ml/util/` | Accepted | 2026-06-09 | The stream-seam intake moves to `ml/util/` so serving/realtime reuse one frame-intake without depending on `demo/` (strict `demo → util`, guard-tested). Model-seam, playback, and overlay stay in `demo/` (YAGNI). References ADR-005. |
| [ADR-007](./ADR-007-ml-local-filesystem-layout.md) | `ml/` local filesystem layout — weight cache + derived outputs | Accepted (partially superseded by ADR-012) | 2026-06-09 | Two gaps no prior ADR owned: upstream pose weights cache to `ml/weights/` (ephemeral, re-downloadable) instead of the project root; generated outputs live under `ml/data/` output-role subdirs (`annotated/`, reserved `eval/`). MECE vs ADR-003/004/005/006 via permanence + data-role-by-subdir discriminators. Complements ADR-004. |
| [ADR-008](./ADR-008-issue-driven-worktree-enforcement.md) | Issue-driven worktrees, enforced git-natively from one source of truth | Accepted | 2026-06-09 | One issue → one branch `<type>/<issue#>-<slug>` → one worktree. Enforcement is git-native (`core.hooksPath` → `.githooks/`) backed by single-source POSIX scripts in `scripts/git-guard/` that every layer (git hooks, Claude, Codex) invokes — identical behavior by construction. Protected-branch commits/pushes refused; stale push blocked, stale commit warns; `git wt` alias as front door; `GIT_GUARD_PROTECTED=` escape hatch. Rejected husky (Node-coupled), agent-only (bypassable), remote-only (too late). |
| [ADR-009](./ADR-009-fall-classification-strategy.md) | Fall-classification strategy — keypoint-sequence temporal models; public datasets first | Accepted | 2026-06-09 | Rule-based bbox-geometry classifier failed 0/8 on real top-down gold clips — structural, not threshold (post-fall `aspect_ratio` stays < 1.2; before/after distributions overlap; person detection drops on floor). Classifier content = learned temporal models (LSTM/Transformer/TCN) over COCO-17 keypoint sequences. Data strategy: public video datasets first (track 2b, #40), VLM-labeling of own footage deferred (2c). gold-8 = locked 0/8 baseline. Fills the seam ADR-005 left open. |
| [ADR-010](./ADR-010-realtime-live-inference-demo-mode.md) | Real-time per-frame live inference as the standard demo observation mode | Accepted | 2026-06-09 | Pre-rendered annotated-video playback replaced by live per-frame inference rendered into a Streamlit placeholder (overlay + fall state as each frame is processed). Recorded-clip live playback first (reuse `VideoFileSource`, ADR-006); camera/RTSP later. Small surgery — pipeline is already frame-by-frame. Implementation tracked in #39. |
| [ADR-011](./ADR-011-live-camera-intake-and-multipage-demo.md) | Live camera intake as a second `FrameSource`, on a separate demo page | Accepted | 2026-06-10 | `CameraSource` joins `VideoFileSource` in `ml/util/frame_source.py` (infinite stream, wall-clock `time_sec`, latest-frame-first); live camera viewing is a separate Streamlit multipage page (file vs camera loop semantics are disjoint); camera selection = index-probe + thumbnail, no device-name dependency. Fulfils ADR-006, extends ADR-010. Rejected: same-page source branch, pyobjc name enumeration, streamlit-webrtc. |
| [ADR-012](./ADR-012-ml-data-domain-first-layout.md) | Domain-first two-tier layout for `ml/data/` + access boundary | Accepted | 2026-06-10 | `ml/data/` partitions domain-first (`{nursing-home,le2i,…}/{raw,processed,poses,annotated}` with a mandatory role vocabulary); cross-domain `eval/` and transient `uploads/` are the only top-level non-domain entries. Partially supersedes ADR-004 (input location → domain-scoped; gitignore + raw-is-sacred inherited) and ADR-007 (MECE row 6: domain-bound derived outputs move inside domains; `ml/weights/` untouched). Access boundary: `nursing-home/` is operator-only, `uploads/` is the sole externally reachable surface, `FALL_DEMO_MODE` defaults to `public` (fail-safe). |
| [ADR-013](./ADR-013-le2i-training-pipeline-decisions.md) | Le2i training-pipeline decisions — dataset detail, window labelling, recall-first threshold, gold-clip evaluation | Accepted | 2026-06-10 | Implementation-level contracts under ADR-009's Track 2b: Le2i chosen over UP-Fall (Lying-class collision, download gate); windows T=30/stride=5 labelled positive iff overlap ≥ 0.5 with 1-based-inclusive → 0-based-half-open interval mapping; clip-wise split (0.25) with disjointness asserts; operating threshold = Recall ≥ 0.90 point persisted to `metadata.json` (skew-tolerant readers); mandatory gold-clip secondary eval (pos-window fraction ≥ 0.5) against ADR-009's rule-based 0/8 floor. |
| [ADR-014](./ADR-014-fail-fast-error-policy.md) | Fail-fast error policy — explicit refusal over silent fallback | Accepted | 2026-06-10 | When code cannot fulfill its contract it refuses with a typed exception/Error; fake fallbacks (defaults/mocks/empty results masking failure) are forbidden in production paths. Enforcement is mechanical (ruff/eslint/tsc/jscpd deny-list, owned by `docs/rules/code-stability.md`); broad catches legal only at process boundaries with logged traceback + justification; intentional degradation must be loud (logged + alerting). Excludes ML inference-quality monitoring (separate layer). Rejected: graceful-degradation default, advisory-only guide, defensive defaults, wholesale NASA P10/Tiger Style. Grounded in `docs/research/code-stability-enforcement-practices.md`; implemented via plan `code-stability-enforcement` (#51). |
